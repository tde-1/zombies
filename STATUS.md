# Where things stand

**Current state lives in one place: the top table of `docs/kickstart/next-session.md`.** This file is a
pointer so the two can never disagree. Launcher 0.2.36 (client DLL `a02958f8`), box DLL `59577dbe`,
host `9e9e86a` at the 2026-09-24 01:25 UK handoff; the next-session table wins over this line.

History (every older STATUS section, kept as written): `docs/kickstart/history/status-history.md`.
Read a part of it only when a pointer sends you there.
